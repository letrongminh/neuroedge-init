# RFC-0005 (principle 2) — a prompt-injected hour-long unlock: 60 s → 3600 s.
schema:  neuroedge.gate/v1
name:    widens-argument-maximum
version: 1.0.0
extends: neuroedge://gates/args-base@1.0.0
arguments:
  duration_s: { type: integer, maximum: 3600 }
