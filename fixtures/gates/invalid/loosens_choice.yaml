# Principle 2 — admits `web`, which the base excluded.
schema:  neuroedge.gate/v1
name:    loosens-choice
version: 1.0.0
extends: neuroedge://gates/strict-base@1.0.0
allow_when:
  request_channel: { in: [in_person, app, web] }
